# 微信智能体助手（小微）— 项目汇报

> 汇报日期：2026-08-06
> 技术负责人：全栈团队
> 项目阶段：核心功能已完成，持续迭代中

---

## 一、项目概述

### 1.1 项目背景

**微信智能体助手「小微」** 是一个基于大语言模型的智能助理系统，旨在通过 AI Agent 技术为用户提供全方位的智能服务。项目以微信 IM 和桌面端（Tauri）为双通道入口，集成了 ReAct Agent 工作流、MCP 工具生态、多模态交互、语音合成、文档处理等核心能力。

### 1.2 项目目标

- **双端触达**：支持微信实时对话 + 桌面客户端两种交互方式
- **智能问答**：基于大模型的多轮上下文理解与自然语言交互
- **工具生态**：集成 20+ 实用工具 + 多个垂直领域 MCP Agent
- **安全可控**：读/写工具分级确认、操作可追溯
- **多模态**：文本、图片、语音、文件全类型输入处理

### 1.3 项目价值

| 维度 | 价值体现 |
|------|---------|
| **用户体验** | 自然语言交互、流式回复、语音播报、可视化思维链 |
| **业务覆盖** | 日常问答、出行打车、文档处理、招聘求职、外卖点餐、电商服务 |
| **技术创新** | ReAct Agent 编排、MCP 协议集成、行为路径自学习、多 Agent 并行 |
| **工程质量** | 模块化架构、断路器保护、全链路指标监控 |

---

## 二、技术架构

### 2.1 整体架构图

> 📌 **PPT 制作建议**：此架构图可替换为专业架构图软件（如 draw.io）绘制的可视化版本，建议突出数据流向和模块分层。

```
┌─────────────────────────────────────────────────────────┐
│                      用户入口层                          │
│  ┌──────────────┐    ┌──────────────────────────────┐   │
│  │  微信 IM     │    │  桌面客户端 (Tauri + React)   │   │
│  └──────┬───────┘    └──────────────┬───────────────┘   │
│         │                           │                    │
│  ┌──────┴───────────────────────────┴───────────────┐   │
│  │              API 网关 / 消息分发层                 │   │
│  │         (HTTP REST + SSE / iLink SDK)             │   │
│  └──────────────────────┬───────────────────────────┘   │
│                         │                               │
│  ┌──────────────────────┴───────────────────────────┐   │
│  │              AI Agent 引擎层                      │   │
│  │  ┌─────────────────────────────────────────────┐ │   │
│  │  │ 中央 Orchestrator (意图路由)                │ │   │
│  │  │  ┌─────────┐  ┌──────────┐  ┌───────────┐ │ │   │
│  │  │  │General  │  │   Taxi    │  │  FileSystem│ │ │   │
│  │  │  │ Agent   │  │  Agent    │  │  Agent     │ │ │   │
│  │  │  └─────────┘  └──────────┘  └───────────┘ │ │   │
│  │  │  ┌─────────┐  ┌──────────┐                 │ │   │
│  │  │  │  Job    │  │McDonald's│                 │ │   │
│  │  │  │ Agent   │  │  Agent   │                 │ │   │
│  │  │  └─────────┘  └──────────┘                 │ │   │
│  │  └─────────────────────────────────────────────┘ │   │
│  │                                                   │   │
│  │  LangGraph4j StateGraph (ReAct 循环)             │   │
│  └──────────────────────┬───────────────────────────┘   │
│                         │                               │
│  ┌──────────────────────┴───────────────────────────┐   │
│  │              工具 / 技能 / MCP 生态层              │   │
│  │  ┌────────────┐ ┌────────────┐ ┌──────────────┐ │   │
│  │  │ 本地工具    │ │  Skills    │ │  MCP 源      │ │   │
│  │  │ (20+@Tool) │ │ (8个技能)  │ │(滴滴/麦当劳…)│ │   │
│  │  └────────────┘ └────────────┘ └──────────────┘ │   │
│  └──────────────────────┬───────────────────────────┘   │
│                         │                               │
│  ┌──────────────────────┴───────────────────────────┐   │
│  │              基础服务层                           │   │
│  │  ┌────────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌─────┐ │   │
│  │  │MySQL  │ │Redis │ │LLM   │ │TTS   │ │VL   │ │   │
│  │  └────────┘ └──────┘ └──────┘ └──────┘ └─────┘ │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术栈详情

#### 后端技术栈

| 分类 | 技术选型 | 版本 | 用途 |
|------|---------|------|------|
| **语言** | Java | JDK 21 | 主开发语言 |
| **框架** | Spring Boot | 3.2.5 | Web / IoC / 配置 |
| **ORM** | MyBatis | 3.0.3 | 数据持久化 |
| **数据库** | MySQL | 8.0 | 会话 / 消息 / 用户存储 |
| **缓存** | Redis | 7.x | 会话缓存（可选） |
| **AI 框架** | LangChain4j | 1.7.1 | LLM 工具绑定 |
| **Agent 编排** | LangGraph4j | 1.8.18 | ReAct 图工作流 |
| **大模型** | DeepSeek | deepseek-v4-pro | 核心对话推理 |
| **多模态** | Qwen-VL | qwen-vl-plus | 图片理解 |
| **语音合成** | DashScope (CosyVoice) | v3-plus | TTS 语音输出 |
| **MCP SDK** | MCP Java SDK | 0.8.1 | 工具协议集成 |
| **微信接入** | iLink SDK | 2.3.3 | 微信 Bot 能力 |
| **文档解析** | PDFBox + Apache POI | 3.0.1 / 5.3.0 | PDF/Word 解析 |
| **向量检索** | Qdrant | — | 文档语义检索 |
| **前端框架** | React | 18.3 | UI 开发 |
| **桌面壳** | Tauri | 2.x | 桌面应用封装 |
| **构建工具** | Vite | 5.4 | 前端构建 |
| **Rust** | Rust | 1.77+ | Tauri 后端 |

#### 前端技术栈

| 分类 | 技术选型 | 用途 |
|------|---------|------|
| **UI 框架** | React 18 + JSX | 组件化 UI 开发 |
| **Markdown 渲染** | react-markdown + remark-gfm | AI 回复渲染 |
| **SSE 通信** | 原生 fetch + ReadableStream | 流式对话 |
| **桌面 API** | @tauri-apps/api | 系统通知 / 窗口管理 |
| **HTTP 通信** | fetch + Tauri HTTP 协议 | 前后端通信 |

### 2.3 模块架构

```
wechat-ai-assistant (Maven 多模块)
├── wechat-ai-common       # 通用组件：枚举、常量、工具类
├── wechat-ai-session      # 会话 / 消息持久化、提醒任务
├── wechat-ai-document     # 文档上传、解析、向量检索
├── wechat-ai-tool         # 工具系统：@Tool 扫描、Skills、MCP
├── wechat-ai-agent        # 子 Agent 调用门面（AgentInvoker）
├── wechat-ai-ai           # AI 引擎：LangGraph 编排、LLM、TTS
├── wechat-ai-wechat       # 微信接入：iLink SDK、消息收发
├── wechat-ai-proactive    # 行为路径自学习（可选）
└── wechat-ai-starter      # 启动入口与全局配置
```

**模块依赖关系：**

```
starter → session / document / ai / tool / wechat
ai      → session + document + tool + agent + proactive
agent   → tool + ai(接口)
wechat  → session + document + ai
tool    → document + common
*       → common
```

---

## 三、功能模块详解

### 3.1 功能全景图

```
┌─────────────────────────────────────────────────────┐
│                   微信智能体助手 功能地图              │
├─────────────┬───────────────┬───────────────────────┤
│  对话引擎    │   工具生态     │    多模态交互          │
├─────────────┼───────────────┼───────────────────────┤
│ • 多轮对话   │ • 天气查询    │ • 图片识别 (VL)        │
│ • 意图路由   │ • 翻译服务    │ • 语音合成 (TTS)       │
│ • 流式回复   │ • 股票行情    │ • 文档解析 (PDF/Word)  │
│ • 会话历史   │ • 快递查询    │ • 文件上传与检索       │
│ • 自动命名   │ • 网页阅读    │ • 附件预览             │
│ • 思维链    │ • 浏览器操作  │ • 拖拽上传             │
│             │ • 内容转换    │ • 多轮上下文           │
│             │ • 文档生成    │                       │
├─────────────┼───────────────┼───────────────────────┤
│  Agent 体系  │   外部集成    │    系统能力            │
├─────────────┼───────────────┼───────────────────────┤
│ • General   │ • 微信 Bot    │ • 提醒任务             │
│ • Taxi      │ • 滴滴 MCP   │ • 邮件发送             │
│ • FileSystem│ • 麦当劳 MCP │ • 指标监控             │
│ • Job       │ • 猎聘 MCP   │ • 用户管理             │
│ • McDonald's│ • Qdrant     │ • 多账号支持           │
│ • 并行执行   │ • 百度翻译   │ • 断路器保护           │
│ • 分级确认   │ • DashScope │ • 自动熔断             │
└─────────────┴───────────────┴───────────────────────┘
```

### 3.2 核心功能模块

#### 3.2.1 对话引擎（AI Agent）

**架构：** 基于 LangGraph4j 的 StateGraph 实现 ReAct 循环

```
START → prepare_input → session_prepare → llm_think
                              │          │
                              │    needTool?
                              │     ┌────┴────┐
                              │  tool_execute  END
                              │     │
                              └─────┘
```

**关键特性：**
- **中央 Orchestrator 模式**：中央 LLM 只负责意图路由，将任务分发给 5 个专业子 Agent
- **多 Agent 并行执行**：`ParallelAgentBatchService` 支持多个写操作并行执行，确认排队处理
- **断路器保护**：连续 3 次 400 错误或 8 次其他错误自动熔断，防止雪崩
- **消息膨胀检测**：单次对话超过 150 条消息自动熔断
- **会话历史滑窗**：保留最近 30 轮对话，防止上下文过长
- **孤儿 tool 链修复**：自动检测并修复不完整的 tool_calls/tool 响应链

#### 3.2.2 工具系统

**工具架构：** 本地 @Tool + MCP 外部工具 + Skills 技能

| 类别 | 数量 | 说明 |
|------|------|------|
| **本地 @Tool** | 20+ | 天气、翻译、股票、快递、文档、浏览器等 |
| **MCP 工具** | 3 个源 | 滴滴出行、麦当劳点餐、猎聘招聘 |
| **Skills 技能** | 8 个 | 天气、翻译、快递、文档生成、邮件等 |
| **Agent 工具** | 5 个 | General、Taxi、FileSystem、Job、McDonalds |

**写入工具（需用户确认）：**
- `setRemind` — 定时提醒
- `sendEmail` — 邮件发送
- `generateDocument` — 文档生成
- `taxi_create_order` — 打车下单
- `mcdonalds` 系列 — 点餐下单

#### 3.2.3 多模态交互

| 模态 | 技术方案 | 能力范围 |
|------|---------|---------|
| **文本** | DeepSeek LLM | 多轮对话、上下文理解、文本生成 |
| **图片** | Qwen-VL Plus | 图片描述、文字识别、内容理解 |
| **语音** | CosyVoice TTS | 语音合成（4种音色）、MP3 输出 |
| **文件** | PDFBox + POI | PDF / Word / TXT 解析、向量检索 |
| **网页** | Playwright + Jsoup | 网页读取、截图、内容提取 |

#### 3.2.4 前端桌面应用

**核心组件：**

| 组件 | 功能 |
|------|------|
| `App.jsx` | 主应用：会话管理、SSE 通信、状态编排 |
| `ReasonRail.jsx` | 思维链面板：实时展示 AI 推理过程 |
| `RouteMapView.jsx` | 路线地图视图：出行路线可视化 |
| `api.js` | API 层：SSE 解析、会话缓存、后端通信 |
| `confirm.jsx` | 确认弹窗：写操作二次确认 |
| `remind.jsx` | 系统提醒：桌面通知管理 |

**UI 布局：**
```
┌─────────────────────────────────────────────┐
│  ┌─────────┐  ┌──────────────────────────┐  │
│  │         │  │                          │  │
│  │ NavBar  │  │       聊天主窗口          │  │
│  │ (56px)  │  │                          │  │
│  │         │  │  ┌────────────────────┐  │  │
│  ├─────────┤  │  │  消息区域          │  │  │
│  │         │  │  │                    │  │  │
│  │         │  │  ├────────────────────┤  │  │
│  │ Left    │  │  │  输入区域          │  │  │
│  │ Panel   │  │  └────────────────────┘  │  │
│  │(可调宽) │  │                          │  │
│  │         │  │  ┌────────────────────┐  │  │
│  │         │  │  │  思维链(Right)    │  │  │
│  │         │  │  └────────────────────┘  │  │
│  └─────────┘  └──────────────────────────┘  │
└─────────────────────────────────────────────┘
```

**特色功能：**
- **思维链可视化**：AI 执行过程中实时流式展示推理步骤（ReasonRail）
- **路线地图**：打车/路线规划结果可视化展示（RouteMapView）
- **使用统计**：Token 消耗、响应时长、工具调用次数双折线图
- **欢迎页**：随机任务提示词 + 新闻推荐卡片
- **会话自动命名**：基于首条消息自动生成会话标题
- **本地缓存**：会话消息 localStorage 缓存，后端离线可继续浏览

---

## 四、关键技术实现

### 4.1 ReAct Agent 工作流

**实现方式：** LangGraph4j StateGraph + 中央 Orchestrator + 子 Agent

```java
// ChatGraphConfig.java — 图定义
new StateGraph<>(ChatGraphState::new)
    .addNode("prepare_input", prepareInputNode)
    .addNode("session_prepare", sessionPrepareNode)
    .addNode("llm_think", llmThinkNode)
    .addNode("tool_execute", toolExecuteNode)
    .addEdge(START, "prepare_input")
    .addEdge("prepare_input", "session_prepare")
    .addEdge("session_prepare", "llm_think")
    .addConditionalEdges("llm_think",
        state -> state.isNeedTool() ? "tool" : "done",
        Map.of("tool", "tool_execute", "done", END))
    .addConditionalEdges("tool_execute",
        state -> state.awaitConfirm() ? "confirm" : "llm",
        Map.of("llm", "llm_think", "confirm", END));
```

**创新点：**
- 中央 LLM 仅看 Agent 白名单（generalAgent / taxiAgent 等），实现关注点分离
- 子 Agent 自包含小 ReAct 循环，有独立的工具白名单和最大轮数限制
- `AgentInvoker` 统一封装子 Agent 调用，支持超时和轮数控制

### 4.2 断路器与熔断保护

```java
// LlmService.java
private static final int CIRCUIT_TRIP_400 = 3;    // 连续 3 次 400 → 拉闸
private static final int CIRCUIT_TRIP_ERROR = 8;  // 连续 8 次其他错误 → 拉闸
private static final int MESSAGE_BLOAT_THRESHOLD = 150; // 消息数阈值

// 熔断后自动恢复机制（冷却期重置）
// 400 错误触发会话历史清理，防止污染上下文
```

**保护层次：**
1. **消息膨胀检测**：对话超过 150 条触发熔断
2. **400 错误计数**：连续 3 次触发会话清理 + 熔断
3. **通用错误计数**：连续 8 次触发熔断
4. **冷却期自动恢复**：CIRCUIT_COOLDOWN_MS 后自动允许重试

### 4.3 MCP 协议集成

**实现方式：** 原生 HTTP JSON-RPC 2.0（无需官方 SDK）

```java
// DidiMcpSource.java
// 1. 获取工具列表
POST {mcp_url}  { "jsonrpc": "2.0", "method": "tools/list", "id": 1 }

// 2. 调用工具
POST {mcp_url}  {
  "jsonrpc": "2.0",
  "method": "tools/call",
  "params": { "name": "taxi_search", "arguments": {...} },
  "id": 2
}
```

**支持的 MCP 源：**

| MCP 源 | 协议 | 工具数 | 说明 |
|--------|------|--------|------|
| **滴滴出行** | HTTP JSON-RPC | 8+ | 打车、路线、地点搜索 |
| **麦当劳** | HTTP JSON-RPC | 10+ | 点餐、优惠券、积分 |
| **猎聘招聘** | HTTP JSON-RPC | 5+ | 简历、职位搜索、投递 |
| **文件系统** | stdio (Node) | 6 | 文件读写、目录操作 |

**自动发现机制：** `McpClientManager` 在 `@PostConstruct` 阶段自动扫描所有 `McpSource` 实现类，建立全局工具索引。

### 4.4 工具动态注册

```java
// ToolRegistryImpl.java — 自动扫描 @Tool 注解
@PostConstruct
public void init() {
    String[] beanNames = applicationContext.getBeanDefinitionNames();
    for (String beanName : beanNames) {
        Object bean = applicationContext.getBean(beanName);
        for (Method method : beanClass.getMethods()) {
            Tool toolAnn = method.getAnnotation(Tool.class);
            if (toolAnn != null) {
                // 自动提取工具定义、参数 Schema
                toolMap.put(toolName, new ToolEntry(def, bean));
            }
        }
    }
}
```

**分级体系：**
- `READ` — 只读工具，直接执行（查询天气、翻译等）
- `WRITE` — 写入工具，需用户确认（发邮件、下单、生成文档等）
- `SYSTEM` — 系统工具，特殊处理（askHuman、exitConversation）

### 4.5 流式对话实现

**后端：** Spring SSE (Server-Sent Events)
```java
// ChatController.java — SSE 流
@PostMapping("/send-stream")
public SseEmitter sendStream(@RequestBody ChatSendReq req) {
    // LangGraph 执行 → 逐步推送 delta/progress/done 事件
}
```

**前端：** 原生 ReadableStream 消费
```javascript
// api.js — SSE 消费
async function consumeSse(res, { onDelta, onProgress, onDone, signal }) {
    const reader = res.body.getReader();
    while (true) {
        const { done, value } = await reader.read();
        // 解析 event/data 对，分发给对应回调
    }
}
```

### 4.6 并行 Agent 执行

```java
// ParallelAgentBatchService.java
// 多 Agent 并行执行 → 按停下先后排队确认 → 汇总结果
public ResumeOutcome onResumeResult(String sessionId, AgentResult result) {
    // 1. 处理当前确认项
    // 2. 检查队列中是否还有待确认项
    // 3. 全部完成后触发 aggregate 汇总
}
```

**设计亮点：**
- 并行运行：多个写操作 Agent 同时执行
- 确认排队：按停止时间排序依次确认
- 动态恢复：确认后可继续执行下一个 Agent

### 4.7 行为路径自学习

```java
// 行为路径自学习模块
// CriticEvaluator → PathEmbeddingService → PathMaintenanceScheduler
// 热路径（confidence > 0.75）直接复用工具链
// 新路径存入 Qdrant，下次相似场景检索复用
```

---

## 五、数据库设计

### 5.1 核心表结构

#### session 表（会话）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 自增主键 |
| session_id | VARCHAR(36) | 业务 UUID |
| user_id | VARCHAR(64) | 用户标识 |
| title | VARCHAR(100) | 会话标题 |
| source | ENUM | WECHAT / API |
| status | ENUM | ACTIVE / EXPIRED / DELETED |
| message_count | INT | 消息条数 |
| last_message_at | DATETIME | 最近消息时间 |
| created_at | DATETIME | 创建时间 |
| expire_at | DATETIME | 过期时间 |

#### message 表（消息）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 自增主键 |
| message_id | VARCHAR(36) | 消息 UUID |
| session_id | VARCHAR(36) | 会话 ID |
| role | ENUM | USER / ASSISTANT |
| message_type | ENUM | TEXT / IMAGE / FILE / TOOL_CALL |
| content | TEXT | 消息内容 |
| file_ids | JSON | 附件 ID 列表 |
| tool_calls | JSON | 工具调用数组 |
| finish_reason | VARCHAR(32) | STOP / TOOL / NEED_CONFIRM / ERROR |
| latency_ms | INT | 响应耗时 |
| created_at | DATETIME | 创建时间 |

#### document 表（文档）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 自增主键 |
| doc_id | VARCHAR(36) | 文档 UUID |
| user_id | VARCHAR(64) | 上传用户 |
| file_name | VARCHAR(255) | 文件名 |
| file_size | BIGINT | 文件大小 |
| file_type | VARCHAR(32) | PDF / DOC / DOCX / TXT |
| chunk_count | INT | 分块数量 |
| created_at | DATETIME | 上传时间 |

#### reminder 表（提醒）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 自增主键 |
| reminder_id | VARCHAR(36) | 提醒 UUID |
| session_id | VARCHAR(36) | 关联会话 |
| trigger_at | DATETIME | 触发时间 |
| content | TEXT | 提醒内容 |
| status | ENUM | PENDING / FIRED / CANCELLED |
| repeat_rule | VARCHAR(128) | 重复规则 |

---

## 六、实施进度

### 6.1 完成情况总览

| 模块 | 进度 | 状态 |
|------|------|------|
| **项目脚手架** | 100% | ✅ 完成 |
| **会话管理** | 100% | ✅ 完成 |
| **消息持久化** | 100% | ✅ 完成 |
| **AI Agent 引擎** | 95% | ✅ 核心完成 |
| **工具系统** | 95% | ✅ 核心完成 |
| **MCP 集成** | 90% | ✅ 3 源已接入 |
| **微信接入** | 90% | ✅ 核心完成 |
| **桌面客户端** | 85% | ✅ 核心完成 |
| **多模态交互** | 90% | ✅ 核心完成 |
| **语音合成** | 100% | ✅ 完成 |
| **断路器/监控** | 95% | ✅ 完成 |
| **文档处理** | 90% | ✅ 核心完成 |
| **行为自学习** | 60% | 🔄 迭代中 |

### 6.2 里程碑时间线

| 阶段 | 主要工作 | 状态 |
|------|---------|------|
| **M1: 基础架构** | 多模块脚手架、会话/消息持久化、微信 SDK 接入 | ✅ |
| **M2: AI 引擎** | LangGraph 编排、LLM 集成、工具注册、ReAct 循环 | ✅ |
| **M3: 工具生态** | 20+ 本地工具、Skills 技能系统、读写分级 | ✅ |
| **M4: MCP 扩展** | 滴滴/麦当劳/猎聘 MCP 源接入、MCP 客户端框架 | ✅ |
| **M5: 子 Agent** | General/Taxi/FileSystem Agent、并行执行、确认排队 | ✅ |
| **M6: 桌面端** | Tauri 壳、React UI、SSE 流式、思维链可视化 | ✅ |
| **M7: 多模态** | Qwen-VL 图片、CosyVoice TTS、文档 RAG | ✅ |
| **M8: 稳定性** | 断路器、熔断、消息膨胀保护、链路监控 | ✅ |
| **M9: 体验优化** | 自动命名、欢迎页、路线地图、使用统计 | ✅ |
| **M10: 持续迭代** | 行为自学习、更多 MCP 源、移动端适配 | 🔄 |

---

## 七、遇到的问题与解决方案

### 7.1 核心问题汇总

| 编号 | 问题 | 影响 | 解决方案 | 状态 |
|------|------|------|---------|------|
| P01 | LLM 400 错误导致上下文污染 | 高频请求时对话崩溃 | 400 触发会话历史清理 + 熔断保护 | ✅ |
| P02 | 消息链断裂（tool_calls 缺少 tool 响应） | AI 无法继续推理 | `fixOrphanedToolCalls` 自动修复 | ✅ |
| P03 | 连续 assistant(tool_calls) 合并问题 | 400 insufficient tool messages | `collapseConsecutiveAssistantToolCalls` | ✅ |
| P04 | 流式渲染 Markdown 不完整 | 用户看到残缺内容 | `patchMarkdown` 语法补全 | ✅ |
| P05 | Tauri Windows 上传不稳定 | 文件上传失败 | base64 JSON 替代 multipart | ✅ |
| P06 | MCP 大文件写入超限 | 写工具 Range out of bounds | 临时文件 + 分块写入策略 | ✅ |
| P07 | 并行 Agent 确认队列混乱 | 写操作执行顺序错乱 | 按停止时间排序 + 动态恢复 | ✅ |
| P08 | 微信语音气泡限制 | 无法直接语音输出 | MP3 文件替代方案 | ✅ |
| P09 | LLM 消息膨胀导致 API 拒绝 | 长对话上下文溢出 | 30 轮滑窗 + 截断修复 | ✅ |
| P10 | 前端 SSE 中断后状态残留 | 重新发送卡住 | AbortController + 清理机制 | ✅ |

### 7.2 重点方案详解

#### 问题 P01：LLM 400 错误熔断保护

**问题：** 高频请求时 API 可能返回 400 错误（上下文过长、格式异常等），若不处理会导致对话持续失败。

**解决方案：**
```java
// LlmService.java
// 1. 连续 400 计数 + 会话清理
if (c400 >= CIRCUIT_TRIP_400) {
    circuitOpen = true;
    sessionHistoryMapper.clearSessionHistory(sessionId); // 清理污染上下文
}

// 2. 熔断后返回友好提示
return Map.of("content",
    circuitOpen ? "AI 服务已熔断，请重启应用恢复"
                : "AI 服务暂时不可用，请稍后重试");
```

#### 问题 P02：Tool 链自动修复

**问题：** 会话历史加载时可能出现 assistant(tool_calls) 缺少对应 tool 响应的情况，导致 API 返回 400。

**解决方案：** `fixOrphanedToolCalls` 方法实现三级修复：
1. 合并连续的 assistant(tool_calls)（只保留最后一条）
2. 扫描消息链，标记不完整的 tool_calls
3. 回退到最后一个完整的 user 消息处，裁掉所有孤儿链

#### 问题 P04：Markdown 流式补全

**问题：** 流式渲染 Markdown 时，由于 chunk 切割导致语法不完整（如未闭合的代码块、加粗等）。

**解决方案：**
```javascript
// 前端 patchMarkdown 函数
// 实时补全：代码块 ```、行内代码 `、加粗 **、斜体 *、删除线 ~~、表格
// 流式结束时强制完整渲染
```

---

## 八、风险评估与应对措施

### 8.1 技术风险

| 风险项 | 影响等级 | 发生概率 | 应对措施 |
|--------|---------|---------|---------|
| **LLM API 不稳定** | 🔴 高 | 中 | 断路器 + 熔断 + 降级提示 |
| **MCP 外部服务宕机** | 🟡 中 | 低 | 超时控制 + 友好降级文案 |
| **微信 SDK 版本更新** | 🟡 中 | 低 | 抽象适配层 + 版本锁定 |
| **数据泄露/安全** | 🔴 高 | 极低 | 密钥环境变量注入 + HTTPS |
| **Token 成本超预期** | 🟡 中 | 中 | 消息滑窗 + 长度截断 |
| **多模态质量波动** | 🟢 低 | 中 | 多模型备份 + 质量阈值 |

### 8.2 业务风险

| 风险项 | 影响 | 应对措施 |
|--------|------|---------|
| **用户留存** | 核心指标 | 欢迎页引导、使用率统计、反馈收集 |
| **功能复杂度** | 开发效率 | 模块化迭代、MVP 优先 |
| **工具过度依赖** | 功能稳定性 | 本地工具优先、MCP 降级方案 |

### 8.3 安全措施

| 维度 | 措施 |
|------|------|
| **密钥管理** | 环境变量注入，禁止硬编码 |
| **操作确认** | WRITE 类工具强制二次确认 |
| **输入过滤** | 参数校验 + Schema 验证 |
| **日志脱敏** | API Key 仅显示前 4 位 + "***" |
| **权限隔离** | 用户会话隔离，防止越权访问 |
| **CORS 限制** | 仅允许桌面端 + 开发域名 |

---

## 九、团队协作

### 9.1 分工架构

| 角色 | 职责 | 产出 |
|------|------|------|
| **架构设计** | 整体架构、技术选型、模块划分 | 多模块 Maven 结构、AI Agent 架构 |
| **AI 引擎** | LangGraph 编排、LLM 集成、ReAct 实现 | ChatGraphConfig、LlmService |
| **工具开发** | 本地工具、MCP 源、Skills | 20+ 本地工具、4 个 MCP 源 |
| **微信集成** | iLink SDK、消息流水线 | 微信 Bot 收发、多账号支持 |
| **桌面开发** | Tauri 壳、React UI、SSE 通信 | 完整桌面客户端 |
| **数据层** | MySQL Schema、MyBatis 映射 | 会话/消息/提醒持久化 |
| **测试/运维** | 断路器、指标监控、故障排查 | UsageMetrics、断路器保护 |

### 9.2 协作模式

- **分支策略**：Git 分支管理，功能分支独立开发
- **代码评审**：PR 审查，确保代码质量
- **接口契约**：前后端 API 文档统一维护
- **配置管理**：application.properties 环境隔离

---

## 十、资源使用情况

### 10.1 代码规模统计

> 📊 **数据来源**：基于项目代码静态分析估算（精确统计需通过脚本扫描，此处为开发过程中的实际规模评估）

| 维度 | 数量 | 说明 |
|------|------|------|
| **Maven 模块** | 9 个 | common / session / document / tool / agent / ai / wechat / proactive / starter |
| **Java 源文件** | ~80+ | 含 Controller / Service / Entity / Config / Mapper 等 |
| **前端组件** | ~15 个 | App / ReasonRail / RouteMapView / confirm / remind 等 |
| **后端 API 端点** | 30+ | 覆盖 chat / session / document / tools / desktop / mail / tts |
| **本地 @Tool** | 20+ | 天气、翻译、股票、快递、文档、浏览器、邮件等 |
| **MCP 源** | 4 个 | 滴滴 / 麦当劳 / 猎聘 / 文件系统 |
| **子 Agent** | 5 个 | General / Taxi / FileSystem / Job / McDonalds |
| **Skills 技能** | 8 个 | 天气 / 翻译 / 快递 / 文档 / 邮件 / 提醒等 |
| **数据库表** | 6+ | session / message / document / remind / notification / app_user |

### 10.2 技术资源

| 资源 | 使用量 | 说明 |
|------|--------|------|
| **JDK 21** | 核心运行时 | 虚拟线程、模式匹配、性能优化 |
| **Spring Boot 3.2.5** | Web 框架 | REST / SSE / IoC / 自动配置 |
| **MySQL 8.0** | 主存储 | InnoDB 引擎，会话/消息/用户 |
| **Redis 7.x** | 可选缓存 | 会话缓存、幂等去重（Caffeine） |
| **DeepSeek API** | LLM 推理 | deepseek-v4-pro，核心对话引擎 |
| **阿里云 DashScope** | TTS / VL | CosyVoice v3-plus / Qwen-VL Plus |
| **百度 AI** | 翻译 | 多语言翻译 API |
| **Qdrant** | 向量存储 | 文档语义检索、行为路径存储 |
| **LangChain4j 1.7.1** | AI 框架 | LLM 工具绑定、Prompt 模板 |
| **LangGraph4j 1.8.18** | Agent 编排 | StateGraph / ReAct 循环 |
| **MCP SDK 0.8.1** | 协议集成 | 工具发现与调用 |
| **iLink SDK 2.3.3** | 微信接入 | Bot 收发、扫码登录 |
| **Tauri 2.x** | 桌面壳 | 原生窗口、系统通知 |

### 10.3 计算资源

| 资源 | 配置建议 | 说明 |
|------|---------|------|
| **后端服务** | 2C4G+ | Spring Boot JAR 运行 |
| **MySQL** | 独立实例 | 主数据存储，定期备份 |
| **Redis**（可选） | 1G+ | 会话缓存，非强制依赖 |
| **Tauri 桌面** | 本地运行 | 用户桌面环境，内存占用 < 300MB |

---

## 十一、未来规划

### 11.1 短期规划（1-2 月）

| 方向 | 具体内容 |
|------|---------|
| **Agent 扩展** | 接入更多 MCP 源（银行、航空、医疗） |
| **性能优化** | 引入 Redis 缓存热点会话、异步工具执行 |
| **移动端** | 开发移动端适配版本（React Native 或 PWA） |
| **文档完善** | API 文档、部署文档、用户手册 |
| **测试覆盖** | 核心模块单元测试 + 集成测试 |

### 11.2 中期规划（3-6 月）

| 方向 | 具体内容 |
|------|---------|
| **多模态增强** | 视频理解、OCR 增强、表格识别 |
| **知识库** | 企业知识接入、私有文档训练 |
| **用户画像** | 偏好学习、个性化回复 |
| **多 Agent 协同** | 复杂任务多 Agent 协作完成 |
| **开放平台** | 对外 API、开发者社区 |

### 11.3 长期愿景

| 方向 | 具体内容 |
|------|---------|
| **平台化** | 从"工具集"进化为"AI Agent 平台" |
| **生态建设** | 插件市场、第三方 Agent 接入 |
| **行业方案** | 教育、医疗、金融等垂直行业解决方案 |
| **开源社区** | 核心模块开源、社区共建 |

---

## 十二、附录

### 12.1 API 端点一览

| 前缀 | 模块 | 主要端点 |
|------|------|---------|
| `/api/v1/chat` | 对话 | send / send-stream / image / image-stream / file / file-stream / history |
| `/api/v1/session` | 会话 | list / create / title / delete |
| `/api/v1/document` | 文档 | upload / parse / search / summary |
| `/api/v1/tools` | 工具 | list / toggle / history |
| `/api/v1/desktop` | 桌面 | notifications / usage / reason / suggestions |
| `/api/v1/mail` | 邮箱登录 | send-code / verify-code |
| `/api/v1/tts` | 语音合成 | synthesize |

### 12.2 配置项清单

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `server.port` | 服务端口 | 8081 |
| `wechat.ai.llm.model` | LLM 模型 | deepseek-v4-pro |
| `tts.model` | TTS 模型 | cosyvoice-v3-plus |
| `mcp.didi.enabled` | 滴滴 MCP 开关 | false |
| `mcp.mcdonalds.enabled` | 麦当劳 MCP 开关 | false |
| `mcp.liepin.enabled` | 猎聘 MCP 开关 | false |
| `mcp.filesystem.enabled` | 文件系统 MCP 开关 | false |
| `wechat.watchdog.tool-timeout-ms` | 工具超时 | 60000 |
| `wechat.watchdog.agent-timeout-ms` | Agent 超时 | 240000 |

### 12.3 目录结构速查

```
wechat-ai-assistant/
├── wechat-ai-common/        # 通用组件
├── wechat-ai-session/       # 会话持久化
├── wechat-ai-document/      # 文档处理
├── wechat-ai-tool/          # 工具系统
├── wechat-ai-agent/         # 子 Agent
├── wechat-ai-ai/            # AI 引擎
├── wechat-ai-wechat/        # 微信接入
├── wechat-ai-proactive/     # 行为学习
├── wechat-ai-starter/       # 启动入口
└── desktop/                 # Tauri 桌面端
```

---

> **声明**：本报告基于项目源代码静态分析生成，具体数据以实际部署环境为准。

